server:
  port: 8088

spring:
  application:
    name: gdg-service-admin
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/gdg_auth
    username: postgres
    password: JOJOloj
    driver-class-name:  org.postgresql.Driver
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_shema: admin_schema


jwt:
  secret: sage-meliodas-GAZ-2026-secret-key-secure-2556bits
  expiration: 3600000

