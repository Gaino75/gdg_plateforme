spring.application.name=gdg-service-auth
server.port=8081

# ══════════════════════════════════════
# CONFIG LOCAL (active pendant le développement)
# ══════════════════════════════════════

# spring.datasource.url=jdbc:postgresql://localhost:5432/gdg_plateforme
# spring.datasource.username=postgres
# spring.datasource.password=12345
# spring.datasource.driver-class-name=org.postgresql.Driver

# ══════════════════════════════════════
# CONFIG SUPABASE (à activer au déploiement)
# Décommente ces lignes et commente celles du dessus
# ══════════════════════════════════════
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-0-eu-west-3.pooler.supabase.com:6543/postgres}
spring.datasource.username=${DATABASE_USERNAME:postgres.lfndwsyzicnktfxmgxhw}
spring.datasource.password=${DATABASE_PASSWORD:gdg@2026!Secure}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.default_schema=auth_schema
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

jwt.secret=gpg_jwt_secret_key_2025_very_long_and_secure_key
jwt.expiration=3600000