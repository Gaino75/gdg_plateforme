server:
  port: 8081
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: always

spring:
  application:
    name: gdg-service-auth
  cloud:
   config:
     enabled: false
  datasource:
   url: 
   username: 
   password: 
   driver-class-name: 

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: sage-meliodas-GAZ-2026-GDG-plateforme-secure-key
  expiration: 3600000

  