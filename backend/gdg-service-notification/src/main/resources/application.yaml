server:
  port: 8087

spring:
  application:
    name: gdg-service-notifications
  
  cloud:
    config:
      enabled: false
  
  # ============================================================
  # DATASOURCE - Utiliser UNE des deux configurations
  # ============================================================
  
  # Configuration pour la base de données locale (TEST)
  datasource:
    url: jdbc:postgresql://localhost:5432/gdg_plateforme
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
  
  # Configuration pour Supabase en ligne (PRODUCTION) - Commentée
  # datasource:
  #   url: jdbc:postgresql://aws-0-eu-west-3.pooler.supabase.com:6543/postgres
  #   username: postgres.lfndwsyzicnktfxmgxhw
  #   password: gdg@2026!Secure
  #   driver-class-name: org.postgresql.Driver
  #   hikari:
  #     maximum-pool-size: 10
  #     minimum-idle: 2

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        default_schema: notifications_schema

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  mail:
    host: smtp.gmail.com
    port: 587
    username: sagemeliodas@gmail.com
    password: apqa zjir ifjm ryxg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# ============================================================
# JWT
# ============================================================
jwt:
  secret: sage-meliodas-GAZ-2026-secret-key-secure-256bits

# ============================================================
# TWILIO (pour SMS) - Commenté car non configuré
# ============================================================
# twilio:
#   account-sid: ${TWILIO_ACCOUNT_SID:}
#   auth-token: ${TWILIO_AUTH_TOKEN:}
#   phone-number: ${TWILIO_PHONE_NUMBER:}

# ============================================================
# LOGGING
# ============================================================
logging:
  level:
    com.gdg: DEBUG
    org.springframework.web: INFO

# ============================================================
# PARAMÈTRES APPLICATION
# ============================================================
app:
  notifications:
    duree-expiration-reservation: 30
    nb-signalements-confirmation: 2
    delai-signalement-heures: 2